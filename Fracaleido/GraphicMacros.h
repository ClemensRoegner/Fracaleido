////////////////////////////////////////////////////////////////////////////////////////
//
// Useful macros for rendering
//
////////////////////////////////////////////////////////////////////////////////////////

#pragma once

#define SAFE_RELEASE(p) if(p) { p->Release(); p = NULL; }

#define CHECK_HR(h) \
	if ( FAILED(h) ) { \
        return;\
    }